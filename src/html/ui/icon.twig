{% set props = ({
  width: "20",
  height: "20",
  href: "",
  class: "",
})|merge(props ?: {}) %}

<svg width="{{ props.width }}" height="{{ props.height }}" class="icon{{ props.class ? " " ~ props.class }}">
	<use href="@sprite#{{ props.href }}"/>
</svg>
